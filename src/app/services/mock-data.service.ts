// @deprecated — Mock data service is no longer used.
// Hosts and services are now managed via the backend API.
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class MockDataService {}
